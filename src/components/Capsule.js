import React from "react";
import "./capsule.css";
const Capsule=(props)=>{
       return(
        <div className="Capsule">
            {props.text}
        </div>
       )
}

export default Capsule;